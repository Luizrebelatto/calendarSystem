import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    controlsRow: { 
        flexDirection: 'row', 
        alignItems: 'center', 
        paddingHorizontal: 10, 
        marginBottom: 6 
    },
    row: { 
        flexDirection: 'row', 
        minHeight: 48, 
        borderBottomWidth: 1, 
        borderColor: '#eee' 
    },
    dropdown: { 
        width: 90, 
        marginRight: 8 
    }
})