import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    listContainer: {
        paddingTop: 58,
      },
      textCard: {
        color: "#f2dab5"
      },
      card: {
        marginBottom: 8,
        marginHorizontal: 16,
        backgroundColor: "#b4a081"
      },
      empty: {
        textAlign: 'center',
        marginTop: 32,
        color: '#999',
      },
})